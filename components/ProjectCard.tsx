import Link from'next/link';import type{Project}from'@/data/projects';
export default function ProjectCard({project}:{project:Project}){return <Link href={`/work/${project.slug}`} className="card block rounded-2xl p-6"><div className="mb-8 flex items-center justify-between"><span className="kicker">{project.category}</span><span className="mono text-xs text-slate-500">FEATURED</span></div><h3 className="text-2xl font-semibold">{project.name}</h3><p className="mt-3 min-h-14 text-sm leading-6 text-slate-400">{project.description}</p><div className="mt-6 flex flex-wrap gap-2">{project.technologies.map(t=><span key={t} className="rounded-full border border-white/10 px-3 py-1 text-xs text-slate-300">{t}</span>)}</div><div className="mt-8 text-sm font-semibold text-sky-300">VIEW CASE STUDY →</div></Link>}
